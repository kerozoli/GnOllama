<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.gnollama"
          path="/org/gnome/shell/extensions/gnollama/">

    <key name="server-url" type="s">
      <default>'http://localhost:11434'</default>
      <summary>Ollama server URL</summary>
      <description>The base URL of the Ollama server to monitor (e.g. http://localhost:11434).</description>
    </key>

    <key name="poll-interval" type="i">
      <range min="5" max="300"/>
      <default>30</default>
      <summary>Poll interval in seconds</summary>
      <description>How often (in seconds) to query the Ollama server for status updates.</description>
    </key>

  </schema>
</schemalist>
